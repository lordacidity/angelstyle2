// Route marker for /template/<style> (twitter | caption | carousel). StudioShell
// reads the style from the URL and drives the selection; an unknown style just
// falls back to the picker.
export default function Page() {
  return null;
}
